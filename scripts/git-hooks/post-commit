#!/bin/sh
ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
command -v node >/dev/null 2>&1 || exit 0
[ -d "$ROOT/node_modules" ] || exit 0
node "$ROOT/scripts/desktop-refresh.mjs" --hook post-commit >/dev/null 2>&1 </dev/null
exit 0
