{
  "name": "@autobroker/db",
  "version": "0.0.0",
  "private": true,
  "description": "AutoBroker product DB persistence layer — Drizzle ORM over better-sqlite3, drizzle-kit-pull-introspected schema, and the test_run_records cost/time ledger; Mastra runtime state lives separately in mastra.db.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./migrator": {
      "types": "./dist/migrator.d.ts",
      "import": "./dist/migrator.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "db:pull": "drizzle-kit pull",
    "db:generate": "drizzle-kit generate",
    "db:check": "drizzle-kit generate --name ci_empty_diff_gate && git diff --exit-code drizzle/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "drizzle-orm": "~0.45",
    "better-sqlite3": "^12"
  },
  "devDependencies": {
    "drizzle-kit": "~0.31",
    "@types/better-sqlite3": "^7",
    "typescript": "^5.6",
    "vitest": "^4.0.0"
  }
}
