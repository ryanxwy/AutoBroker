{
  "name": "@autobroker/workflows",
  "version": "0.0.0",
  "private": true,
  "description": "Layer 3 — self-built ~50-line SkillRun state machine + HarnessWorkflowRuntime reversible seam (Mastra upgrade slot). Orchestrates the 17 skills; owns awaiting_approval / resume_payload crash-and-resume; drives the L2 in-process gate but never touches SQLite or external APIs directly.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@autobroker/core": "workspace:*",
    "@autobroker/model": "workspace:*",
    "@autobroker/tools": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
