{
  "name": "autobroker",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first, provider-agnostic new-car quote pipeline. Full-TypeScript rebuild of AutoBroker around 17 skills.",
  "type": "module",
  "//ai": "Pinned to ^6 deliberately. Do NOT take v7: its LanguageModelV4 spec breaks V3-spec community providers (ts-migration 2026-05-29 D5/D9). CI must ignore the ai major-bump.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:pull": "drizzle-kit pull",
    "db:generate": "drizzle-kit generate",
    "db:check": "pnpm --filter @autobroker/db run db:check",
    "lint:deps": "depcruise packages apps harness",
    "check:strings": "bash scripts/check-forbidden-strings.sh",
    "harness": "node -e \"console.error('harness runner: TODO(phase-0) — see harness/README.md'); process.exit(1)\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "dependency-cruiser": "^17.4.3",
    "typescript": "~5.6.3",
    "vite": "^6.0.0",
    "vitest": "^4.0.0"
  },
  "dependencies": {
    "ai": "^6"
  }
}
