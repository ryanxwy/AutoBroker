{
  "name": "@autobroker/server",
  "version": "0.0.0",
  "private": true,
  "description": "Layer 5 (app) — backend HTTP + SSE skill-run stream. Owns the embedded-host boot-recovery routine (telemetry kill -> deterministic tool re-registration -> suspended-run re-attach -> restart()/cancel() per age policy). Placeholder until Phase-0 spike 2.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@autobroker/core": "workspace:*",
    "@autobroker/tools": "workspace:*",
    "@autobroker/workflows": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
