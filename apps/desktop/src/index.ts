/**
 * @autobroker/desktop — Layer 5 optional Electron shell. PHASE-6 PLACEHOLDER.
 *
 * Phase 6 (optional) wraps the @autobroker/server process and the
 * @autobroker/ui SPA in an Electron shell for a single-click local app. Until
 * Phase 6 is reached this package intentionally contains nothing — no Electron
 * dependency is installed, keeping the workspace install lean.
 */

// TODO(phase-6): Electron main/preload wiring around server + ui.
export {};
