{
  "name": "@autobroker/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Layer 5 (app) — optional Electron shell around server + ui (Phase 6). Placeholder only; no Electron toolchain until Phase 6 is reached.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
