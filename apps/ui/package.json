{
  "name": "@autobroker/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Layer 5 (app) — React/Vite + AI SDK UI chat rail (dashboard SPA). Placeholder; the real Vite + React toolchain lands with the first dashboard-facing skill.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@autobroker/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
