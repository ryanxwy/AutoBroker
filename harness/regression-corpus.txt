# Accepted L2 regression corpus — re-run via scripts/regression.sh.
# One case file per line (lane comes from the case's own [narrative] lane).
# Frozen at P2. POST-P2 REALITY: skills accepted after P2 are gated by the
# deterministic *.func.toml cases in the ui:functional lane + the /e2e-loop live
# journeys — NOT by growing this list. Only a READ-ONLY live L2 case (no send, no
# irreversible mutation) is eligible to enter this corpus. *.deferred.toml and the
# cross-provider smoke cases (anthropic/openai, deferred in dev period) stay out.
# DeepSeek-only, read-only / approve_safe skills only.

# search_profile_intake — accepted 2026-06-05 (api) + UI-lane revalidation 2026-06-10
search_profile_intake.slash.toml
search_profile_intake.freeform.toml
search_profile_intake.decline.toml
search_profile_intake.ui_slash.toml
search_profile_intake.ui_freeform.toml
search_profile_intake.ui_decline.toml

# dealer_geosearch — accepted 2026-06-10 (UI-lane two rounds); STOP cases accepted 2026-06-12 (profile-guard slice)
dealer_geosearch.ui_narrative.toml
dealer_geosearch.ui_button.toml
dealer_geosearch.stop_intake_cta.toml
dealer_geosearch.stop_picker.toml

# inventory_site_scan — accepted 2026-06-12 (full 7-step loop). The scan now
# auto-scans every in-radius rooftop (no gate, no decline path — owner 2026-06-23),
# so the dealer_pipeline prefix below carries its always-on slot; the ui_button
# popover round stays rotated out (one full site-pass per guardian run is the budget).
dealer_pipeline.ui_prefix.toml

# inventory_link_scan — accepted 2026-06-12 (UI-lane two rounds + decline; seeds verified against the live Maps world)
# ui_button stays accepted-but-rotated (2 rooftop contacts per run — politeness budget)
inventory_link_scan.ui_decline.toml

# incentive_scrape — accepted 2026-06-13 (full 7-step loop, P2 关账). New OEM
# sources are now auto-approved (no first-encounter gate — owner 2026-06-23), so
# ui_first_encounter (one OEM-page contact + ~$0.005 extraction LLM per run,
# true-path Δ cash rows) carries the always-on slot.
incentive_scrape.ui_first_encounter.toml

# dealer_reply_extract — accepted 2026-07-03 (read-only live L2 extraction). The
# only corpus-eligible read-only live acceptance case sitting in no other
# automated lane; the geosearch/intake rotation-pool edges stay deliberately out.
dealer_reply_extract.live_extract.toml
