# Dependencies
node_modules/

# Package manager local stores
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Secrets / local env
.env
.env.local

# SQLite (data dir lives under ~/.autobroker-ts during parity; never commit DBs)
*.db
*.db-wal
*.db-shm

# Test / coverage artifacts
coverage/

# Desktop shell self-contained bundle (generated by apps/desktop/scripts/bundle.mjs)
apps/desktop/bundle/

# Local scratch / temp
tmp/
.tmp/
temp/
*.tmp

# Machine-local Codex config / hooks
.codex/
.agents/
AGENTS.md

# skill-loop session marker (created by /skill-loop, removed at acceptance;
# the optional Stop-hook gate keys its full-green scope off this file)
.claude/.skill-loop-active

# OS
.DS_Store

# Cold-copied legacy SQLite for drizzle-kit pull (copy-not-share scratch)
.cold-copy/
